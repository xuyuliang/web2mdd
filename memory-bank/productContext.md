### 软件的启动
软件首次启动需要时间较长，这可以理解。
不过软件怎么知道自己是首次启动？软件在启动后应该检查，首次启动的工作是否已经做完，如果做完，就跳过，增加启动速度。
同时，软件应该保留一个启动参数，不论怎样都重做首次启动的工作。

### 获得词频的值

我举个例子：比如查询单词 splice，结果中，第一个RANK后面的那个数字（这个单词是20290）就是它的词频。
RANK 不止一个，所以词频值不止一个。但是我们只取第一个。

把词频信息单独抽出来，不需要在web页面上显示它，只需要做成api接口就可以了。
