# Product Context

各个功能点的详细需求描述。

---

### 1. 获得词频的值

**状态**: ✅ 已实现

**实现**: `WordFreq.get_rank(word) -> int | None`

加载 COCA60000.txt（每行一个单词，按频率降序排列），用行号作为单词的排名（1-based）。在模式搜索结果的单词旁显示 COCA 词频排名，排名不存在则不显示。

```python
rank = word_freq.get_rank("splice")  # 返回 14588
```


---

### 2. 支持简单的正则表达式查字典

**状态**: ⏳ 待实现

（待补充详细需求）

---

### 3. 能找出单词的词根词缀，同源词

**状态**: 🔶 部分已实现

#### 3a. 词根词缀切分 ✅ 已实现

- 从 `_prefixes.txt`（149个前缀）和 `_suffixes.txt`（198个后缀）加载数据
- 在 `/api/lookup` 精确匹配时自动进行词根词缀分析
- 按长度降序匹配，优先匹配更长的词缀
- 支持词干有效性验证（防止误切，如 "beautiful" 不被切掉 "be-"）
- 支持单字符后缀保护（防止 "hello" 被切 "-o"）
- 在查词结果下方展示拆分结果（前缀-词干-后缀，不同颜色高亮）
- 可折叠展开词干的词典释义

#### 3b. 同源词查找 ⏳ 待实现

（待补充详细需求）

---

### 4. 能根据拼写，查找拼写相似的单词

**状态**: ⏳ 待实现

（待补充详细需求）
