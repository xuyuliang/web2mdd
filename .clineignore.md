# 排除我自己的数据文件夹

The little dict/

# 排除大型依赖文件夹

node_modules/
**pycache**/
.venv/
venv/
env/
.git/

# 排除大型数据文件（防止 AI 被海量数据淹没）

_.mdd
_.mdx
_.sqlite
_.db
_.csv
_.log

# 排除构建产物

dist/
build/

# 排除敏感信息（安全第一）

.env
_.pem
_.key
