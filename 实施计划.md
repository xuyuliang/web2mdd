# 实施计划：吞音/垫音感知的"查找高频词缀"管线

> 目标：改造 `splits.json → segment-freqs.json → highfreq.json` 这条"查找高频词缀"链，
> 加入垫音（padding）/吞音（elision）变体识别与合并，让同一词根词缀的多个表面形式共享频次，
> 产出更准确、更完整的高频词缀表。
>
> 边界：**roots.json 只读、不改**；不做 PMI/熵 10 万词典大清洗；不做模型侧 xxing/.le 硬规则（另立任务）。

## 当前链路（已核实）
1. `scripts/build_segment_freqs.py`：从 `data\anki_splits.json`（原 `数据资料\splits.json`，来自 `mydata.anki2` 的 4149 条手工切分）抽段计数 → `数据资料\segment-freqs.json`（3210 段，**无位置信息**）
2. `scripts/build_confirmed_affixes.py`：取 ≥3 次的段，比对 `data\high-freq-affixes.json`（456 手工）+ `data\roots.json`（只读）→ 输出（已改为 `data\anki_highfreq.json`）
3. `app\word_cutter.py:_secondary_score`（word_cutter.py:165）用该频次做加分

## 问题（实测）
`ty`(7) 与 `ity`(33)、`tion`(42) 与 `ation`(49)、`dis`(33) 与 `di`(19)、`spel`(1) 与 `spell`(0)、`dor`(3) 与 `dors`(0)
当前被当成独立词缀分别计数，频次被拆散，还可能各自达不到阈值被漏掉。

## 已完成（第 0 步）
- `数据资料/splits.json` → `data/anki_splits.json`
- `plains/gold-standard.csv` → `data/anki_gold_standard.csv`（校验集）
- `数据资料/highfreq.json` → `data/anki_highfreq.json`（快照；重跑管线会再生）
- 路径切换：
  - `scripts/build_segment_freqs.py` 源 → `data\anki_splits.json`
  - `scripts/build_confirmed_affixes.py` 输出 → `data\anki_highfreq.json`
  - `app/word_cutter.py:_load_high_freq` 读取 → `data\anki_highfreq.json`

---

## 第 1 步：位置感知（build_segment_freqs.py）
- 按 split 串的 `.` 边界，记录每个段出现的相对位置：词首 / 词尾 / 中段。
- 输出 `segment-freqs.json` 时每个段带 `pos`（可多值，如首又尾）。
- 供第 2 步变体合并限定"同位置"，防止 `in-`（前缀）与 `-in`（后缀）误并。

## 第 2 步：吞/垫变体检测 + 合并（build_confirmed_affixes.py，范围=方案 A）
只做两类，**不**做元音区变化的根变体（`abdom/abdomin/abdomen` 这类留待以后）。

- **垫音 pad**：同位置、Levenshtein 距离 = 1（仅单字母插/删）、长度差 = 1 →
  `ty/ity`、`ob/obs`、`dis/di`、`tion/ation`
- **吞音 elide**：双写塌缩，b 由 a 删掉某个 `(.)\1` 双写中的 1 个 → `spell/spel`、`dorss/dors`
- 排除翻转对（LD=2）：`re/er`、`de/ed` 一律不得合并

合并逻辑：
- 构图 + 传递闭包 → 连通分量；分量内频次求和。
- 规范头优先级：手工 `high-freq-affixes.json` 已收录 → `roots.json` forms 有收录 → 最长/最高频。
- meaning/lang 从 `roots.json` 或手工表**只读继承**。
- 阈值 ≥3 改为**合并后**再过滤，避免拆散变体各自不足 3 被漏掉。
- 输出 `data\anki_highfreq.json` 增加元信息：`variants`（如 `["ty","ity"]`）、`variant_type`（pad/elide）。

## 第 3 步：校验
- 断言示例变体对合并正确：`ty/ity`、`tion/ation`、`dis/di`、`spel/spel`(吞)、`dor/dors`(吞)。
- 确认反面不放行：`re/er`、`de/ed` 不得合并。
- 用 `data/anki_gold_standard.csv` 抽验合并结果是否符合手工切分。
- 对比合并前后 `anki_highfreq.json` 的条数与频次分布，人工抽查无"错误合并"。

## 边界/明确不做
- roots.json 不动
- 不做文档里的 PMI/熵/10 万词典大清洗（本次以 `anki_splits.json` 手工切分当 ground truth，轻量替代）
- 不做模型侧 xx/h/.le 硬规则