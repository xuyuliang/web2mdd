# 实施计划：词素身份表驱动的"查找高频词缀"管线

> 目标：改造 `anki_splits.json → segment-freqs.json → anki_highfreq.json` 这条"查找高频词缀"链，
> 加入垫音（pad）/吞音（elide）/等长辅音替换（assimilation）三类变体识别与合并，
> 把所有表面形归并为"一个词根词缀身份"，让身份统一统计"出现几次"。
>
> 产出走三层：
> 1. `segment-freqs.json`（位置感知，只投影段与数）
> 2. `allomorph_groups.json` + `allomorph_review.json`（**词素身份表**，识别与裁决）
> 3. `anki_highfreq.json`（按身份归并频次后的派生前缀/后缀表，供分词器加分）
>
> 边界：**roots.json 只读、不改**；不做 PMI/熵 10 万词典大清洗；不做模型侧硬规则（另立任务）。

## 当前链路（已实现）
1. `scripts/build_segment_freqs.py`：从 `data\anki_splits.json`（4149 条手工切分，3402 非空）抽段，
   按 `.` 边界记录相对位置 → `数据资料\segment-freqs.json`（3210 段，首/尾/中多维）。
2. `scripts/build_allomorph_groups.py`：构建 **词素身份表**。节点 = (表面形, 词性)，来源三层
   （手工 `high-freq-affixes.json` rank0 / `roots.json` 系 rank1-2 / 语料段 rank3），
   在同词性节点间生成三类规则边 + roots 权威 base→form 边，过安全闸后用并查集分组 → `data\allomorph_groups.json`。
3. `scripts/build_confirmed_affixes.py`：**按身份确认、按表面形报数**。身份合并数 ≥3 才放行整个组；
   每个表面形输出条目，`次数` 为其在语料对应位置的真实次数，`merged_count` 为身份合计 → `data\anki_highfreq.json`。
4. `app\word_cutter.py:_secondary_score`（word_cutter.py:165）用该频次做加分（任一变体命中都享合并频次）。

## 三类规则边
- **pad**：同位置、LD=1（单字母插/删）、长度差=1 → `ty/ity`、`ex/exo`、`ation/tion`。
- **elide**：双写塌缩（删 `(.)\1` 之一）→ `spel/spell`。
- **assimilation**：同长度、仅边界（首/末）单辅音替换 → `ex/ef`、`en/em`、`in/im/il/ir`、`sym/syn`。
- 一律排除翻转对（LD=2）：`re/er`、`de/ed` 不得合并。

## 裁决闸（全部边统一执行，安全第一）
- 释义行两两最大重叠 `line_overlap = 共享词素 / min(行词素数)`，`MIN_OVERLAP=0.5` 过才并。
- **assimilation 额外要求共享 ≥2 词素**：仅靠"相反/离开"这类单个泛义词并线的 `de/re/se` 一律不并。
- 手动已有释义的节点（rank0），roots 释义不参与判定（防 roots 混义污染 `di`）。
- 门不过 / 跨语言 / 无释义 → **隔离进 `allomorph_review.json`，绝不静默合并**。
- 无释义语料段（rank3 count-only）不做规则边。

## 关键裁决（实测正确）
| 案例 | 结果 |
|------|------|
| `e/ef/ex/exo` | 并入一族（pad），共享计数 75 |
| `en/em`、`in/im/il/ir`、`sym/syn`、`ob/oc/of/op` | 各并一族 |
| `ty/ity` | 并入一族（39）；`ation/tion`（85） |
| `dis` | = `dif/dis`；`di` = "二"(希腊) 独立（**不与 dis 混淆**） |
| `de/re/se` | 各自独立（不因共享"相反"误并） |

## 状态
- 第 0 步 归档：`anki_splits.json` / `anki_gold_standard.csv` / `anki_highfreq.json`（快照）+ 路径切换 ✅
- 第 1 步 位置感知重写：`build_segment_freqs.py` → `segment-freqs.json` ✅
- 第 2 步 身份表：`build_allomorph_groups.py`（含三类边、双闸门、review 隔离、`ld_insdel` 已修复支持前缀/中垫）✅
  实测（方案 A 继承后）：nodes=19312、edges_merged=8144、review=11789、groups=11805。
- 第 2.5 步 review 增强（方案 A）：纯语料节点继承同形式手工/词根节点的释义（修 lar@suffix 显示 null）；每条 review 新增 `examples:{a,b}`（例词对象 `{w,seg,src}`，来自 `anki_splits.json` + `rules_clean.jsonl`，field3 优先）与 `positive:""`（留人工填 t）✅
  实测：11789 条全部带 positive；`lar↔lax` 条 a_gloss="A gull"、a 例词 molar/collar/caterpillar/intracellular、b 例词 laxative/lax。继承使新增合并 +93（8051→8144）、review −78。
- 第 3 步 频次投影：`build_confirmed_affixes.py` 按身份归并 → `anki_highfreq.json`（658 条 / 408 身份）✅

## 已验证
- smoke test：`word_cutter` 从新 `anki_highfreq.json` 加载 617 个去重表面形，分词正常。
- **`次数` = 各表面形真实频率**（不虚高），`merged_count` = 身份合计（≥3 过滤依据）：
  `ten` 族：ten=6 / tent=1 / tena,tenaci,tenax,tens=0，merged=7；`ex` 族：ex=37 / ef=5 / exo=1，merged=75；
  `ty`=6 / `ity`=33，merged=39；`ation`=49 / `tion`=36，merged=85。
  `_secondary_score` 按真实频率加分：`ex`→1.406、`ef`→0.684、`exo`(1次)→不加分、`tenax`(0次)→不加分。
  `schematic → schema | t | ic`（符合"按语料高频定 schema"的直觉）。
- 关键裁决全部正确（见上表）。

## 待办（下一步）
- [ ] 用 `data\anki_gold_standard.csv` 抽验合并结果；对比并入前后条数与频次分布；人工抽查无"错误合并"。
- [ ] **schema/scheme 思路落点**：当前"词义判不了、但语料高频"的候选对被 `add_review` 的 `no_gloss` 分支直接丢弃。
      拟加 `corpus_signal`：规则边候选对中任一端 is 手工/词根（rank≤1）且双边语料计数 ≥3、
      即便释义判不了，也录入 review（reason=corpus_signal）供人工确认音变（如 schema↔scheme m/n）。绝不自动并入。
      （待最终确认是否实现。）

## 边界/明确不做
- roots.json 不动
- 元音区变化的根变体（`abdom/abdomin/abdomen`）不做（留待以后）
- 不做文档里的 PMI/熵/10 万词典大清洗（以 `anki_splits.json` 手工切分当 ground truth）
- 不做模型侧 xx/h/.le 硬规则