# torch==2.13.0+cpu CPU 版，必须手动安装
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# 
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
readmdict
numpy==1.26.4
pytest==8.3.4

# ---- 部署服务器（app 运行时必需）----
# 蒸馏模型2 通过 onnxruntime 加载 output/model.onnx，服务器上必须装这个。
onnxruntime

# ---- 仅在本地重训/导出模型时可选 ----
# 服务器只跑 app 则可全部忽略。

onnx==1.22.0
onnxscript==0.7.1